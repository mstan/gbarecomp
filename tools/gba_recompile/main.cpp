// gba_recompile — STUB.
//
// Eventually: given a ROM + game.toml + symbol set, produce one or
// more .c files containing the recompiled ARM/THUMB code. For now a
// placeholder so the build target exists.

#include <cstdio>

int main(int /*argc*/, char** /*argv*/) {
    std::printf("gba_recompile: stub. See docs/ROADMAP.md (Phase 1).\n");
    return 0;
}
